?devtools
renv::install("devtools")
usethis::use_logo("man/figures/logo.png")
devtools::build_readme()
usethis::use_readme_rmd()
usethis::use_logo("man/figures/logo.png")
usethis::use_spell_check()
devtools::build_readme()
devtools::build_rmd()
devtools::build_rmd("README.Rmd")
?devtools::build_rmd
devtools::build_rmd("README.Rmd", quiet = FALSE)
devtools::build_rmd("README.Rmd", quiet = FALSE)
usethis::use_r("raw_data.R")
renv::install("googledrive")
# Get data from Google Drive
library(googledrive)
#---- --- ---- --- ---- --- ---- --- ----#
drive_find(n_max = 10)
#---- --- ---- --- ---- --- ---- --- ----#
# Run basic command to handle authorization
# and API
drive_find(n_max = 10)
rlang::last_trace()
rlang::last_trace(drop = FALSE)
#---- --- ---- --- ---- --- ---- --- ----#
# Run basic command to handle authorization
# and API
drive_find(pattern = "csv")
gargle::gce_instance_service_accounts()
drive_auth(
email = "jpmonteagudo2014@gmail.com",
path = NULL,
subject = NULL,
scopes = "drive",
cache = gargle::gargle_oauth_cache(),
use_oob = gargle::gargle_oob_default(),
token = NULL
)
#---- --- ---- --- ---- --- ---- --- ----#
# Run basic command to handle authorization
# and API
drive_find(pattern = "csv")
token_fetch(scopes = NULL, ...)
gargle::token_fetch(scopes = NULL, ...)
gargle::token_fetch()
#---- --- ---- --- ---- --- ---- --- ----#
# Run basic command to handle authorization
# and API
drive_find(pattern = "csv")
#---- --- ---- --- ---- --- ---- --- ----#
# Run basic command to handle authorization
# and API
drive_find(pattern = "csv")
